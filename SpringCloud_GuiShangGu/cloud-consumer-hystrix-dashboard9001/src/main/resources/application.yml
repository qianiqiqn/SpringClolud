server:
  port: 9001

spring:
  application:
    name: hystrix-management
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7001/eureka/

#management:
#  security:
#    enabled: false
#  endpoints:
#    web:
#      exposure:
#        include: hystrix.stream
#
#feign:
#  hystrix:
#    enabled: true
